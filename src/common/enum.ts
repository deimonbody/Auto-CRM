export enum PATHES {
  LOGIN_PAGE = "/login",
  REGISTER_PAGE = "/register",
  USER_PROFILE = "/user-profile",
  ADMIN_PAGE = "/admin",
  CREATE_TRIP_PAGE = "/create-trip",
}

export enum ROLE {
  PASSENGER = "passenger",
  DRIVER = "driver",
  MANAGER = "manager",
}

export enum UserRole {
  USER = "user",
  ADMIN = "admin",
}
